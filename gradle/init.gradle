allprojects {
    repositories {
        maven {
            url 'https://maven.pkg.github.com/io.github.dqualizer.dqlang'
            credentials(HttpHeaderCredentials) {
                name = 'authorization'
                value = 'Bearer ' + System.getenv('GITHUB_TOKEN')
            }
            authentication {
                header(HttpHeaderAuthentication)
            }
        }
    }
}
